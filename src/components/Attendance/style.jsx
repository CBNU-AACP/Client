import styled from 'styled-components'

const Attendance = styled.div`
  .datagrid {
    items-align: center;
    display: flex;
    height: 100%;
    width: 100%;
    max-width: 30rem;
    .gridparent {
      flexgrow: 1;
      width: 40rem;
    }
  }
`

export default Attendance
